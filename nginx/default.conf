# ---- HTTP (port 80) ----
server {
  listen 80;
  server_name _;

  # ---- Health check (no Node.js dependency) ----
  location /health {
    return 200 "ok";
    add_header Content-Type text/plain;
  }

  # ---- Hashed static assets (immutable, cached forever) ----
  location /_next/static/ {
    alias /app/.next/static/;
    expires 1y;
    add_header Cache-Control "public, immutable";
  }

  # ---- Public assets (images, slides, etc.) ----
  location /public/ {
    alias /app/public/;
    expires 1d;
    add_header Cache-Control "public";
  }

  # ---- Everything else → Next.js SSR ----
  location / {
    proxy_pass http://127.0.0.1:3000;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }
}

# ---- HTTPS (port 443) ----
# To enable HTTPS, place your certificates and uncomment this block:
#   ./ssl/fullchain.pem  — certificate chain
#   ./ssl/privkey.pem    — private key
#
# server {
#   listen 443 ssl;
#   server_name _;
#   ssl_certificate     /etc/nginx/ssl/fullchain.pem;
#   ssl_certificate_key /etc/nginx/ssl/privkey.pem;
#
#   location /health {
#     return 200 "ok";
#     add_header Content-Type text/plain;
#   }
#
#   location /_next/static/ {
#     alias /app/.next/static/;
#     expires 1y;
#     add_header Cache-Control "public, immutable";
#   }
#
#   location /public/ {
#     alias /app/public/;
#     expires 1d;
#     add_header Cache-Control "public";
#   }
#
#   location / {
#     proxy_pass http://127.0.0.1:3000;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#   }
# }
